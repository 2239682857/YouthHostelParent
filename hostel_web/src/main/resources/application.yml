server:
  port: 8072
spring:
  application:
    name: hostel-web
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8070/eureka
  instance:
    prefer-ip-address: true
zuul:
  routes:
    hostel-user: ##用户
      path: /api-user/**
      serviceId: hostel-user
